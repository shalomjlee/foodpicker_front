import React from 'react';

const RandomByCategory = () => {
    return (
        <div>
            <h1>Hello from random Category</h1>
            create an endpoint for restaurant/reandom, let backend pick a restaurantCategories
            endpoint that gives you what want
        </div>
    );
};

export default RandomByCategory;